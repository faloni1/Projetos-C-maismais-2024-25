#include <iostream>

using namespace std;

int main(int argc, char** argv)
{
	setlocale (LC_ALL, "Portuguese");
	double i, n, p, m;
	cout << "Digite 6 números: ";
	for (i=1; i <= 6; i++)
	{m = m + n;
	cin >> n;
	if (n > 0)
		p++;}
	
    cout << p << " valores positivos." << endl << "Média: " << m / 6 << endl;
	
	return 0;
}